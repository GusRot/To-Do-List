import {
    primaryLight,
    secondaryLight,
    textLight,
    buttonLight,
    primaryDark,
    secondaryDark,
    textDark,
    buttonDark,
} from "./Palletes";

export const lightTheme = {
    body: primaryLight,
    secondary: secondaryLight,
    text: textLight,
    button: buttonLight,
};

export const darkTheme = {
    body: primaryDark,
    secondary: secondaryDark,
    text: textDark,
    button: buttonDark,
};
