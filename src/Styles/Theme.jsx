import {
    primaryLight,
    secondaryLight,
    textLight,
    buttonLight,
} from "./Palletes";

export const lightTheme = {
    body: primaryLight,
    secondary: secondaryLight,
    text: textLight,
    button: buttonLight,
};
